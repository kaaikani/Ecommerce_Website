import { map } from './map';
export var createCounter = function () {
    var count = vi.fn();
    return {
        count: count,
        fn: function () {
            return map(function (x) {
                count();
                return x;
            });
        },
    };
};
