export function keys(source) {
    return Object.keys(source);
}
(function (keys) {
    function strict(source) {
        return keys(source);
    }
    keys.strict = strict;
})(keys || (keys = {}));
